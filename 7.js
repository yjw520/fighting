// 连续子数组的最大和
/**
 * @param {number[]} nums
 * @return {number}
 */
 var maxSubArray = function(nums) {
    var sum=0
    var nm=[]
    for(let i=0;i<nums.length;i++){
        sum=sum+nums[i]
    } 
};